# API base URL baked into the JS bundle at export time.
# iOS simulator can use localhost. Android emulator needs 10.0.2.2.
# Physical devices need your machine's LAN IP (e.g. http://192.168.1.10:3000/api/v1).
EXPO_PUBLIC_API_URL=http://localhost:5156/api/v1
EXPO_PUBLIC_APP_NAME=Workspace
